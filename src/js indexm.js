const root=ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <React.StrictMode>
        <App/>
        </React.StrictMode>
    </React.StrictMode>
);
class College extends React .Component{
    render(
        return(){
            return<button onDoubleClick={()=>
            consol.log("Button is clicked")
        }>click here </button>
        }
    )
}
const root=ReactDOM(document.getElementById('root'))
root.render(<college/>)